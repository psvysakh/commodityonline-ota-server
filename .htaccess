DirectoryIndex disabled
RewriteEngine On
RewriteBase /

# Allow actual static files
RewriteCond %{REQUEST_FILENAME} -f
RewriteRule ^ - [L]

# Proxy everything else to Next.js
RewriteRule ^(.*)$ http://127.0.0.1:3000/$1 [P,L,QSA]
